# Условия принятия строки в ДМП-автомате

## Общее правило

Строка считается **принятой**, если выполняются **оба** условия:
1. ✅ Входная строка полностью обработана (пустая)
2. ✅ В стеке остался **только маркер Z** ИЛИ стек **полностью пуст**

**Важно**: Состояние автомата не имеет значения! Строка может быть принята в любом состоянии (q0, q1, q2 и т.д.).

## Примеры

### Пример 1: Язык a^n b^n

Автомат распознает строки с одинаковым количеством 'a' и 'b', где все 'a' идут перед 'b'.

**Переходы:**
```
1. (q0, a, Z) → (q0, aZ)  - первое 'a', кладем в стек
2. (q0, a, a) → (q0, aa)  - следующие 'a', кладем в стек
3. (q0, b, a) → (q1, λ)   - первое 'b', переходим в q1 и убираем 'a'
4. (q1, b, a) → (q1, λ)   - следующие 'b', убираем 'a' из стека
```

**Работа для строки "aabb":**
```
(q0, "aabb", [Z])     - начало
(q0, "abb",  [a,Z])   - применен переход 1
(q0, "bb",   [a,a,Z]) - применен переход 2
(q1, "b",    [a,Z])   - применен переход 3
(q1, "",     [Z])     - применен переход 4
ПРИНЯТА! ✓ - вход пуст, стек содержит только Z, состояние q1
```

### Пример 2: Простое распознавание символа

**Переходы:**
```
1. (q0, a, Z) → (q1, Z)  - читаем 'a' и переходим в q1
```

**Работа для строки "a":**
```
(q0, "a", [Z])  - начало
(q1, "",  [Z])  - применен переход 1
ПРИНЯТА! ✓ - вход пуст, стек содержит только Z, состояние q1
```

**Работа для пустой строки "":**
```
(q0, "", [Z])  - начало
ПРИНЯТА! ✓ - вход пуст, стек содержит только Z, состояние q0
```

### Пример 3: Язык с опустошением стека

**Переходы:**
```
1. (q0, a, Z)  → (q1, λ)  - читаем 'a' и удаляем Z
```

**Работа для строки "a":**
```
(q0, "a", [Z])  - начало
(q1, "",  [])   - применен переход 1
ПРИНЯТА! ✓ - вход пуст, стек пуст, состояние q1
```

## Когда строка НЕ принимается

Строка **отклоняется** в следующих случаях:

❌ **Входная строка не обработана полностью**
```
(q1, "ab", [Z])  - осталось "ab" в входе
ОТКЛОНЕНА - входная строка не пуста
```

❌ **В стеке остались символы (кроме Z)**
```
(q1, "", [a, Z])  - в стеке есть 'a' кроме Z
ОТКЛОНЕНА - в стеке больше чем просто Z
```

❌ **Нет подходящего перехода**
```
(q0, "b", [Z])  - нет перехода для (q0, b, Z)
ОТКЛОНЕНА - не найден переход
```

## Ключевые моменты

1. **Состояние не важно** - строка может быть принята в любом состоянии (q0, q1, q2, ...)
2. **Стек важен** - должен содержать только Z или быть пустым
3. **Вход важен** - должен быть полностью обработан
4. **λ-переходы обрабатываются** - автомат пытается применить λ-переходы, если вход закончился

## Типичная ошибка

❌ **Неправильно:** "Строка принимается только в финальном состоянии q2"
✅ **Правильно:** "Строка принимается в любом состоянии, если вход пуст и стек содержит только Z"

Это важное отличие от конечных автоматов (DFA/NFA), где есть специальные финальные состояния!
