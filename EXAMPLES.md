# Примеры использования ДМП-Автомата

## Пример 1: Палиндромы четной длины (встроенный пример)

Распознает строки вида w·w^R, где w^R - это обращение строки w.

### Таблица переходов

| № | Из состояния | Входной символ | Вершина стека | В состояние | Записать в стек |
|---|--------------|----------------|---------------|-------------|-----------------|
| 1 | q0 | a | Z | q0 | aZ |
| 2 | q0 | b | Z | q0 | bZ |
| 3 | q0 | a | a | q0 | aa |
| 4 | q0 | a | a | q1 | λ |
| 5 | q0 | a | b | q0 | ab |
| 6 | q0 | b | a | q0 | ba |
| 7 | q0 | b | b | q0 | bb |
| 8 | q0 | b | b | q1 | λ |
| 9 | q1 | a | a | q1 | λ |
| 10 | q1 | b | b | q1 | λ |
| 11 | q1 | λ | Z | q2 | λ |

### Принимаемые строки
- `aabbaa` - палиндром
- `abba` - палиндром
- `aa` - палиндром
- `bb` - палиндром
- `baab` - палиндром
- `` (пустая) - палиндром

### Непринимаемые строки
- `abc` - не палиндром
- `aab` - нечетная длина
- `aba` - нечетная длина

## Пример 2: Сбалансированные скобки

Распознает правильные скобочные последовательности.

### Таблица переходов

| № | Из состояния | Входной символ | Вершина стека | В состояние | Записать в стек |
|---|--------------|----------------|---------------|-------------|-----------------|
| 1 | q0 | ( | Z | q0 | (Z |
| 2 | q0 | ( | ( | q0 | (( |
| 3 | q0 | ) | ( | q0 | λ |
| 4 | q0 | λ | Z | q1 | λ |

### Принимаемые строки
- `()` - одна пара
- `(())` - вложенные
- `()()` - последовательные
- `` (пустая) - пустая последовательность

### Непринимаемые строки
- `(` - незакрытая скобка
- `)` - нет открывающей
- `(()` - незакрытая
- `())` - лишняя закрывающая

## Пример 3: Язык a^n b^n

Распознает строки с одинаковым количеством 'a' и 'b', где все 'a' идут перед всеми 'b'.

### Таблица переходов

| № | Из состояния | Входной символ | Вершина стека | В состояние | Записать в стек |
|---|--------------|----------------|---------------|-------------|-----------------|
| 1 | q0 | a | Z | q0 | aZ |
| 2 | q0 | a | a | q0 | aa |
| 3 | q0 | b | a | q1 | λ |
| 4 | q1 | b | a | q1 | λ |
| 5 | q1 | λ | Z | q2 | λ |

### Принимаемые строки
- `ab` - n=1
- `aabb` - n=2
- `aaabbb` - n=3
- `` (пустая) - n=0

### Непринимаемые строки
- `aab` - больше a
- `abb` - больше b
- `ba` - неправильный порядок
- `abab` - чередуются

## Как использовать примеры

1. Откройте приложение
2. Введите переходы в таблицу (или нажмите "Загрузить пример" для примера 1)
3. Введите тестовую строку
4. Нажмите "Запустить"
5. Изучите визуализацию выполнения

## Обозначения

- `λ` (lambda) - пустой символ (используется для ε-переходов или удаления из стека)
- `Z` - начальный символ стека (маркер дна стека)
- `q0` - начальное состояние (всегда)
- `q2` - конечное принимающее состояние (обычно)

## Особенности недетерминизма

Автомат может иметь несколько переходов из одного состояния при одинаковых входных данных.
В этом случае он исследует все возможные пути и сообщает, если хотя бы один путь привел к успеху.

Например, в примере с палиндромами:
- Переходы 3 и 4 дают два варианта для (q0, a, a)
- Переходы 7 и 8 дают два варианта для (q0, b, b)

Автомат "угадывает" середину палиндрома, переходя в состояние q1.
